# Fairly

Quick start (using pnpm):

- Install dependencies: `pnpm install`
- Run the app: `pnpm start`

Notes:

- This project uses `pnpm` as the package manager. If you haven't installed it yet, see https://pnpm.io/installation
